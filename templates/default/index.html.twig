{% extends 'base.html.twig' %}

{% block title %}Lien parent{% endblock %}

{% block body %}
    <div class="container">
        {% for link in links_parent %}
            <div class="row">
                <div class="col s12 center-align">
                    {% if link.link == "" %}
                        {% set href = path('default', {'link_parent': link.id}) %}
                    {% else %}
                        {% set href = link.link %}
                    {% endif %}
                    <a href="{{ href }}" class="waves-effect waves-light btn-large">{{ link.name }}</a>
                </div>
            </div>
        {% endfor %}
        {% if id_current_link != 0 %}
            <div class="row">
                <div class="col s12 center-align">
                    <a href="{{ path('default', {'link_parent': 0}) }}" class="waves-effect waves-light btn-large">Retour</a>
                </div>
            </div>
        {% endif %}
        {% if id_current_link == 0 %}
        <div class="row">
                <div class="col s12 center-align">
                    <a href="{{ path('link_index') }}" class="waves-effect waves-light btn-large">Edit link</a>
                </div>
        </div>
        {% endif %}
    <div>
{% endblock %}